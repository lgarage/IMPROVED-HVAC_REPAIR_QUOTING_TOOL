# Local / secrets (never commit service account JSON)
*-firebase-adminsdk-*.json
serviceAccount*.json
.env
.env.*

# Dependencies (if you add a build step later)
node_modules/

# Firebase local deploy cache (regenerated on every deploy)
.firebase/

# Playwright / screenshot one-offs (never commit)
_test_*.mjs
_check_screenshot.mjs
_vc_pw_verify.mjs
_pw_*.mjs
_ss_*.png
_screenshot_*.png
_pw_*.png
